name: ovox
modules:
  - name: puppetlabs/stdlib
    version_requirement: '>= 9.7 < 10.0.0'
  - name: puppet/openvox_bootstrap
    version_requirement: '>= 1.4.0 < 2.0.0'
  #XXX: Uncomment once live
  ## TODO: switch these once published to forge
  #- git: https://github.com/voxpupuli/puppet-ov_role
  #  ref: main
  #- git: https://github.com/voxpupuli/puppet-ov_profile
  #  ref: main
